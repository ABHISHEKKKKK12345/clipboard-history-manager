# Clipboard History Manager — Python dependencies
# Install with: pip install -r requirements.txt
#
# Hard requirements (app will not start without these)
customtkinter>=5.2
pyperclip

# Strongly recommended (enables Excel .xlsx export)
openpyxl

# Optional (enables global hotkey Ctrl+Shift+V)
pynput
